# 阶段 0：环境说明

> 状态：⬜ 待填充
> 上级文档：[00-总体方案设计](00-总体方案设计.md) · 对应阶段：阶段 0（环境前置）
> 门禁：环境自检通过 —— `soc_version` 确认后方可开始算子编译

## 填写说明

本文件记录**环境事实的核查过程与结论**：实测值是多少、怎么测出来的、有哪些不确定项、如何闭环。
只记录**已发生的事实与已确认的结论**；计划性内容属于 [00-总体方案设计](00-总体方案设计.md) 第 2 节。

**本文档是阶段一的输入** —— 环境未闭环前，后续所有数据都不可信。

## 待补小节

- [ ] **环境事实核查** —— 硬件（芯片型号、卡数 × die、HBM、AI Core）、软件（CANN / ATB / Python / PyTorch / torch_npu）、驱动固件的实测复核，与方案文档 2.1 / 2.2 的记载是否一致
- [ ] **soc_version 确认**（最高优先级）—— 候选取值、验证方法、实测结论、判定依据
- [ ] **HBM 实测带宽与峰值算力** —— 压测方法与实测结果（这是阶段四判定"耗时是否正常"的基准，不能引用规格书数据）
- [ ] **NPU 占用核实** —— 当前显存占用是否为其他用户任务，是否存在邻居干扰
- [ ] **环境自检记录** —— torch / torch_npu 可用性、device_count、NPU 计算自检
- [ ] **工具链核实** —— `msprof` / `msopgen` / `msopprof` / `asys` 等实际可用性与版本
- [ ] **遗留问题与风险** —— 尚未闭环的事项，以及对后续阶段的影响

## 产出物归档

| 产物 | 路径 | 状态 |
|------|------|------|
| 环境自检原始输出 | `logs/` | ⬜ |
| 带宽 / 算力压测数据 | `results/` | ⬜ |
