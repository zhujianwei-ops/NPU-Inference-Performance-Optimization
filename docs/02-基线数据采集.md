# 阶段二：基线数据采集

> 状态：⬜ 待填充
> 上级文档：[00-总体方案设计](00-总体方案设计.md) · 对应阶段：阶段二
> 门禁：**G2** —— 全测试矩阵跑完，性能方差 < 5%

## 填写说明

本文件记录**基线采集的实际执行与基线数据本身**：矩阵怎么取、用什么工具跑的、数据长什么样、哪些点重采过。
只记录**已发生的事实**；计划性内容属于 [00-总体方案设计](00-总体方案设计.md) 第 5 节。

> 基线一经确认即**冻结**。后续任何优化的收益，都以本文档的数据为分母。

## 待补小节

- [ ] **测试矩阵** —— 最终采用的 ISL / OSL / 并发 / 场景取值，以及与方案的差异及原因
- [ ] **采集工具** —— 实际选用的方案（vLLM 内置 / ais_bench / 自研脚本）、脚本位置、选用理由
- [ ] **记录格式** —— 最终确定的 JSON 字段规范（字段含义与必填项）
- [ ] **环境锁定与预热** —— 实际采用的预热次数、调频/swap 锁定措施、温度记录方式
- [ ] **基线数据** —— 每条记录的 `exp_id` 索引表（含模型、并行配置、量化方式、commit、完整启动参数、环境变量、指标分位数、备注）
- [ ] **数据质量** —— 各点的重复次数、方差、异常点识别与重采记录
- [ ] **基线结论** —— **冻结的基线 `exp_id` 列表**，作为后续对比的唯一基准（G2）
- [ ] **问题记录** —— 数据不可比的场景、干扰来源、规避方法

## 产出物归档

| 产物 | 路径 | 状态 |
|------|------|------|
| 基线结构化数据 | `results/baseline/baseline-*.json` | ⬜ |
| 基线可读报告 | `results/baseline/baseline-*.md` | ⬜ |
| 一键采集脚本 | `bench/run_baseline.sh` | ⬜ |
| 结果汇总脚本 | `bench/parse_results.py` | ⬜ |
